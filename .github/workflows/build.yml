name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      submodules: recursive

    - name: Run install script
      run: ./personal_packages.sh -a

      #    - name: Super-Linter
      #uses: github/super-linter@v2.1.0
