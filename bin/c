#!/bin/bash

opts=()
prompt=()

for arg in "$@"; do
  if [[ "$arg" == -* ]]; then
    opts+=("$arg")
  else
    prompt+=("$arg")
  fi
done

if [ ${#prompt[@]} -gt 0 ]; then
  exec claude "${opts[@]}" "${prompt[*]}"
else
  exec claude "${opts[@]}"
fi
